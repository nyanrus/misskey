url: 'http://misskey.local'

setupPassword: example_password_please_change_this_or_you_will_get_hacked

# ローカルでテストするときにポートを被らないようにするためデフォルトのものとは変える(以下同じ)
port: 61812

db:
  host: 127.0.0.1
  port: 54312
  db: test-misskey
  user: postgres
  pass: ''
redis:
  host: 127.0.0.1
  port: 56312
id: aidx

proxyRemoteFiles: true

# Benchmark: allow localhost for mock HTTP server
allowedPrivateNetworks: ['127.0.0.0/8']

# Benchmark: remove rate limiter and increase concurrency to stress ACTIVE list
deliverJobConcurrency: 1024
deliverJobPerSec: 100000
